# CI/CD evidence — GitHub Actions screenshots

Add after pushing PASS and FAIL demo commits:

| File | Description |
|------|-------------|
| `run-pass.png` | GitHub Actions run — green, 0 failed assertions |
| `run-fail.png` | GitHub Actions run — red, 1 failed (`INTENTIONAL CI FAIL DEMO`) |

Capture from: https://github.com/trngnneee/eshop-sut/actions

Also download workflow artifacts `hw06-newman-reports` for `newman-ci-report.html` and `newman-ci.log`.
