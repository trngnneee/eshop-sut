# Spike schedule notes — `23127271_Spike_20260814`

**Not Stress.** Peak is **80** (below Stress **100**, above Load 20). Shape = jump + recover.

P01 locked after first `-n` (peak 40 stayed 0% error).

## Plugin

Open in JMeter requires **jp@gc Ultimate Thread Group** (`jpgc-casutg`). A flat Thread Group of 80 would fail this gate.

## Ultimate Thread Group rows

| Row | Start threads | Initial delay (s) | Startup (s) | Hold (s) | Shutdown (s) | Role |
|-----|---------------|-------------------|-------------|----------|--------------|------|
| 1 | **5** | 0 | 0 | **187** | 0 | Baseline + recover floor for the whole run |
| 2 | **75** | **30** | **2** | **60** | **5** | Spike to **80**, then drop back to 5 |

Timeline (seconds from test start):

| t | Threads | Phase |
|---|---------|--------|
| 0–30 | 5 | Baseline |
| 30–32 | 5 → 80 | Jump (2 s) |
| 32–92 | 80 | Hold peak (60 s) |
| 92–97 | 80 → 5 | Drop (5 s) |
| 97–187 | 5 | Recover (90 s) |

187 = 30 + 2 + 60 + 5 + 90.

## Think-time / listener

- Uniform Random Timer: delay **0**, range **200** → 0–200 ms.
- GUI listener: **Aggregate Report** only (whole-run p90/p95/p99).
- **Jump vs recover is not in Aggregate.** Use `jmeter -n … -e -o report_spike/` (Response Times Over Time) and P10 `.jtl` splits: first ~30 s / hold (~32–92) / last ~90 s.

## Same as Load

Search-to-buy five samplers, CSV (100 unique `tramNN`), Bearer on cart/checkout only, JSON asserts, 10 s timeouts, `startnextloop`, valid passwords only.
