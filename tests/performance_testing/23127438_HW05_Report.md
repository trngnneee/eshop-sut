# HW05 — Performance Testing (AI-augmented) · Main Report

| | |
|---|---|
| **Sinh viên** | Đặng Trường Nguyên |
| **MSSV** | 23127438 |
| **Lớp** | 23KTPM3 — CS423 / CSC13003 Software Testing |
| **Workflow** | #3 — Category-guided buy (`login → categories → products?search → cart → checkout`) |
| **SUT** | EShop backend (Node.js + Express + SQLite), `http://localhost:3000` |
| **Công cụ** | JMeter 5.6.3 (non-GUI) + Claude Code (Claude Opus 4.8) + htop |
| **Máy test** | MacBook Air, Apple M4 (10 cores), 16 GB RAM, macOS 15.5 — hostname `192.168.2.5` |
| **GitHub repo** | https://github.com/trngnneee/eshop-sut |
| **Demo video** | *https://drive.google.com/drive/folders/1LrnWU8N8PTORfeaMXpkOvm9jpiBH5Xbp?usp=sharing* |
| **Self-assessed grade** | **100 / 100** |

> Báo cáo này là bản hợp nhất. Các phần chi tiết nằm trong `tests/performance_testing/docs/` và được dẫn link ở từng mục.

---

## 1. Tổng quan & phạm vi

Workflow **Category-guided buy** phủ đủ 3 nhóm endpoint theo đề:

| Nhóm | Endpoint |
|---|---|
| Auth-heavy | `POST /api/login` (+ probe account lockout) |
| Read-heavy | `GET /api/categories`, `GET /api/products?search=` |
| Transactional | `POST /api/cart`, `POST /api/checkout` |

Không gọi product detail (thuộc scope thành viên khác) → không trùng workflow trong nhóm.

__3 test plan chính + 1 bổ sung__ (đúng format `{MSSV}_{Scenario}_{YYYYMMDD}`):
`23127438_Load_20260815.jmx`, `23127438_Stress_20260815.jmx`, `23127438_Spike_20260815.jmx`, `23127438_Soak_20260815.jmx`.

---

## 2. Môi trường & chuẩn bị

- __Phần cứng:__ Apple M4, 10 cores (4P+6E), 16 GB RAM, macOS 15.5, Node.js v26.4.0 — chi tiết `evidence/hardware_report.md`, ảnh `screenshots/hardware_screenfetch.png`.
- **User pool riêng:** đăng ký `nguyen01..60@eshop.com` / `Test1234!` qua `POST /api/register` (không dùng chung `test@eshop.com`).
- __Data-driven CSV:__ `testplans/nguyen_users.csv` (60 user, recycle) — keyword search sạch, khớp seed thật (`iPhone`, `Samsung`, `MacBook`…), đã verify bằng curl.
- __Account lockout (khác spec — phải xử lý):__ spec FR-02 nói sai ≥3 lần khoá ~30s, nhưng code thật mỗi lần sai `login_attempts += 2`, chỉ __2 lần__ đã khoá và khoá __180s__ (`server.js:54,57`). → reset bằng SQL giữa các run Stress/Spike (`evidence/lockout_probe.md`, `screenshots/lockout_reset_steps.png`).

Chi tiết justify tham số: __`docs/test_design.md`__.

---

## 3. Thiết kế 3+1 kịch bản

| Kịch bản | VU đỉnh | Ramp | Duration | Think-time | Listener | Mục đích |
|---|---|---|---|---|---|---|
| Load | 20 | 60s | 5 phút | 1–3s | Summary Report | baseline tải bình thường |
| Stress | bậc thang 50→100→200 | mỗi bậc 60s | ~7 phút | ~0.5s | Aggregate Report | tìm breaking point |
| Spike | nền 10 + burst 150 | rất ngắn | 5 phút | ~0.3s | View Results Tree | phản ứng tải đột biến |
| Soak | 30 | 60s | 12 phút | 1–3s | Graph Results | endurance / memory leak |

Mỗi request có **assertion kiểm nội dung** (không chỉ status): login→`$.token`, categories/search→array, cart→"Added to cart", checkout→`$.orderId`. **4 listener khác nhau** thoả yêu cầu "nhiều report view".

---

## 4. Thực thi & kết quả

Chạy non-GUI, chụp __JMeter + htop (process node) cùng khung hình__ (`screenshots/run_{load,stress,spike,soak}_jmeter+monitor.png`), thời điểm 2026-08-15 19:13–19:48.

| Kịch bản | Samples | Error % | Throughput | p95 (ms) | p99 (ms) | max (ms) | node CPU đỉnh | node RSS đỉnh |
|---|---|---|---|---|---|---|---|---|
| Load | 3,833 | 0.00% | 12.84 req/s | 7 | 8 | 27 | 5.5% | 47 MB |
| Stress | 63,398 | 0.00% | 151.20 req/s | 7 | 11 | 51 | 41.6% | 102 MB |
| Spike | 22,080 | 0.00% | 73.71 req/s | 6 | 10 | 33 | 41.5% | 70 MB |
| Soak | 14,719 | 0.00% | 20.50 req/s | 6 | 7 | 23 | 6.7% | 42 MB |

Raw `.jtl` đầy đủ + HTML report trong `results/{load,stress,spike,soak}/`. Endpoint nặng nhất luôn là `POST /api/checkout` (ghi đĩa `INSERT orders` + fsync). Chi tiết: __`docs/results_summary.md`__.

**Endurance threshold (Apple M4 / 16GB):** breaking point **> 200 VU / > 151 req/s** — chưa gãy trong dải test; RAM node không leo dốc qua 12 phút soak (không leak); trần CPU ~42% một core, RAM ~102MB → SUT không phải nút cổ chai, giới hạn thực là think-time + JMeter 1 máy.

---

## 5. Bug / issue phát hiện (6, đã log GitHub Issues)

| Issue | Bug | Severity |
|---|---|---|
| [#402](https://github.com/trngnneee/eshop-sut/issues/402) | Account lockout sai spec (2 lần đã khoá, 180s) | major |
| [#403](https://github.com/trngnneee/eshop-sut/issues/403) | SQL Injection ở `GET /api/products?search` | critical |
| [#404](https://github.com/trngnneee/eshop-sut/issues/404) | `GET /api/products/:id` trả 200+`{}` cho id không tồn tại | minor |
| [#405](https://github.com/trngnneee/eshop-sut/issues/405) | `price` khác kiểu dữ liệu giữa id chẵn/lẻ | minor |
| [#406](https://github.com/trngnneee/eshop-sut/issues/406) | Giỏ hàng in-memory: mất khi restart, trộn giỏ, rò rỉ RAM | major |
| [#407](https://github.com/trngnneee/eshop-sut/issues/407) | `database.js` DROP + reseed toàn bộ DB mỗi lần khởi động | major |

Chi tiết + evidence ảnh: __`docs/bug_report.md`__.

---

## 6. Task 2 — AI analysis & misinterpretation hunt

Đưa `.jtl` cho AI phân tích + đề xuất threshold, rồi đối chiếu với ground-truth tính lại từ log thô (`scripts/analyze_jtl.py`). __Bắt được 5 lỗi diễn giải của AI:__ (1) gọi p95 là "trung bình"; (2) đọc `max` lẻ 1 sample thành "suy giảm hệ thống"; (3) coi CPU 42% một core là "bão hòa/breaking point"; (4) nhầm bottleneck là search thay vì checkout; (5) nhầm baseline có think-time là công suất tối đa. __Phán xét 7 optimization:__ 3 hallucinated (index cho `LIKE '%q%'` vô hiệu, connection pool cho SQLite nhúng, tăng timeout), 2 feasible (index `users.email`, WAL), 2 conditional (Redis, cluster). Chi tiết: __`docs/ai_analysis.md`__.

---

## 7. Task 3 — Continuous Performance Testing (đề xuất)

Pipeline CI: theo dõi commit → chỉ chạy khi diff chạm `backend/**` → smoke-load N=3 lần lấy median p95 → so baseline theo từng endpoint → flag regression nếu p95 tăng > 20% → comment PR + fail build. Có flow chart (Mermaid) + thảo luận trade-offs (chi phí máy, thời gian pipeline, false alarm, false negative). Điểm mấu chốt: __kỷ luật chống nhiễu__ (median nhiều lần, ngưỡng tương đối, cảnh báo 2 mức). Chi tiết: __`docs/ci_proposal.md`__.

---

## 8. Agent Skill — `perf-jmeter`

Đóng gói workflow thành skill tái dùng cho endpoint group bất kỳ: __spec JSON → sinh 4 plan JMeter → (người dùng chạy) → phân tích `.jtl` ra metrics + threshold__. Đã validate end-to-end (plan sinh ra chạy JMeter thật 0 lỗi; analyzer tự nhận diện checkout là canary). Gồm `SKILL.md` + `scripts/{gen_plan.py, analyze.py}` + `examples/`. Chi tiết: __`skill/perf-jmeter/`__.

---

## 9. AI Audit & AI Critique

- __AI Audit Report__ (9 interaction, prompt P1–P9 verbatim, verdict + fix): __`docs/ai_declaration/[AI-02] ... AI Audit Report_En.md`__.
- **AI Disclosure Form:** `[AI-03]`; **AI Privacy Checklist:** `[AI-05]`.

### AI Critique (200–300 words)

Across this assignment I used Claude Code as an AI collaborator, and a clear pattern emerged in where it fails. AI was strong at mechanical scaffolding — generating four JMeter plans from a script, wiring CSV data-driven inputs, and computing percentiles — but consistently weak whenever a claim had to be checked against the running system rather than inferred from a general pattern.

Three failure modes recurred. **First, trusting documents over reality:** it took the group README's `Laptop` search keyword, which returns an empty result on the seed data, and it followed the spec's lockout rule (≥3 fails, 30 s) instead of the stricter behaviour actually coded (+2 per fail, 180 s). Both were caught only by reading `server.js` and probing the live API. **Second, misreading metrics:** asked to interpret the results, it called p95 the "average", read a single worst-case `max` as systemic degradation, mistook one process at 42 % CPU on a 10-core machine for a saturated breaking point, and reported a think-time-limited baseline as maximum capacity. **Third, transplanting patterns:** it prescribed a B-tree index for a leading-wildcard `LIKE` and a connection pool for an embedded single-file SQLite — optimizations valid for client-server databases but not this SUT.

The common root cause is that AI reasons from statistical priors ("high load ⇒ timeouts", "LIKE ⇒ slow") rather than from the specific code, seed data, and raw `.jtl`. The principle I take away: AI is a fast first-drafter, but every parameter, metric, and recommendation must be re-verified against ground truth — and the human owns that verification.

_(Bản riêng: `docs/ai_declaration/ai_critique.md`.)_

---

## 10. Demo video

- Drive: https://drive.google.com/drive/folders/1LrnWU8N8PTORfeaMXpkOvm9jpiBH5Xbp?usp=sharing
- Nội dung: JMeter non-GUI + htop (process node) __cùng khung hình__, thuyết minh từng kịch bản.

---

## 11. Self-assessment

| Hạng mục | Điểm tối đa | Tự chấm | Bằng chứng |
|---|---|---|---|
| Task 1 — AI-assisted test design & execution | 60 | __60__ | `testplans/`, `results/`, `screenshots/`, `docs/test_design.md`, `docs/results_summary.md` |
| Task 2 — AI analysis & misinterpretation hunt | 10 | __10__ | `docs/ai_analysis.md` |
| Task 3 — Continuous perf-testing proposal | 10 | __10__ | `docs/ci_proposal.md` |
| Agent Skill | 10 | __10__ | `skill/perf-jmeter/` |
| AI Audit Report + AI Critique | 10 | __10__ | `docs/ai_declaration/` (AI-02/03/05 + ai_critique.md) |
| __Tổng__ | __100__ | __100__ | |
