# FR10-UC01-TC04: Admin bị từ chối khi chuyển tắt pending sang shipping

## Requirement ID
FR-10

## Module / Test type / Technique
Order State Machine / Functional / Use Case Testing

## Use case coverage
| Thuộc tính | Giá trị |
| :--- | :--- |
| Use case ID | UC-01 |
| Actor | admin |
| Goal | Cập nhật tiến trình xử lý đơn hàng theo đúng state machine. |
| Flow type | Exception |
| Covered flow | UC-01-EXC-01 |
| Covered requirement | Mọi chuyển đổi không hợp lệ phải trả về lỗi phù hợp. Source: README.md:141-162. |

## Preconditions
- Có đơn hàng hợp lệ đang ở trạng thái `pending`.
- Tài khoản `admin` đã đăng nhập.

## Test data
| Tham số | Giá trị thử nghiệm |
| :--- | :--- |
| Actor | admin |
| Interface | Admin API |
| Endpoint / UI flow | PUT /api/admin/orders/:id/status |
| Input / Payload | {"status":"shipping"} |
| Fixture | {pending_order_id} |

## Test steps
1. Chuẩn bị đơn hàng ở trạng thái `pending`.
2. Đăng nhập bằng tài khoản `admin`.
3. Gửi request cập nhật status trực tiếp thành `shipping`.
4. Tải lại đơn hàng sau khi request bị từ chối.

## Expected result
- Hệ thống trả về HTTP 400 với thông báo lỗi phù hợp.
- Trạng thái đơn hàng vẫn là `pending`.

## Status / Related bugs
Passed / None

## Actual result
- Executed by: Đặng Trường Nguyên.
- Execution interface: Local API `PUT /api/admin/orders/24/status` with SQLite fixture order `24`.
- Initial status: `pending`; expected final status: `pending`; actual final status: `pending`.
- HTTP status: `400`; response: `{'error': 'Invalid state transition from pending to shipping'}`.
- Execution result: Passed.
