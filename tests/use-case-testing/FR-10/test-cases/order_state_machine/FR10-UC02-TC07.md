# FR10-UC02-TC07: Guest bị từ chối khi gọi API hủy đơn

## Requirement ID
FR-10

## Module / Test type / Technique
Order State Machine / Functional / Use Case Testing

## Use case coverage
| Thuộc tính | Giá trị |
| :--- | :--- |
| Use case ID | UC-02 |
| Actor | guest |
| Goal | Xác minh luồng hủy đơn yêu cầu đăng nhập. |
| Flow type | Exception |
| Covered flow | UC-02-EXC-04 |
| Covered requirement | User hủy đơn qua `PUT /api/orders/:id/cancel`; chỉ khi đơn chưa giao. Source: api_specification.md:141-148. |

## Preconditions
- Có đơn hàng hợp lệ trong hệ thống.
- Không gửi Authorization token.

## Test data
| Tham số | Giá trị thử nghiệm |
| :--- | :--- |
| Actor | guest |
| Interface | API |
| Endpoint / UI flow | PUT /api/orders/:id/cancel |
| Input / Payload | {} |
| Fixture | {existing_order_id} |

## Test steps
1. Không đăng nhập hoặc xóa Authorization token.
2. Gửi request `PUT /api/orders/:id/cancel`.
3. Kiểm tra response và trạng thái đơn hàng.

## Expected result
- Hệ thống trả về HTTP 401.
- Không có trạng thái đơn hàng nào bị thay đổi.

## Status / Related bugs
Passed / None

## Actual result
- Executed by: Đặng Trường Nguyên.
- Execution interface: Local API `PUT /api/orders/33/cancel` with SQLite fixture order `33`.
- Initial status: `pending`; expected final status: `pending`; actual final status: `pending`.
- HTTP status: `401`; response: `{'error': 'Unauthorized'}`.
- Execution result: Passed.
