# Domain Testing & Boundary Value Analysis on EShop

- **Họ và tên**: Đặng Trường Nguyên
- **Mã số sinh viên**: 23127438

## FR-04: Personal profile management -# Domain Testing & BVA

### 1. Name

#### Domain Testing - Bước 1: Xác định biến đầu vào & đầu ra
- Input: `name`
- Output mong đợi: hồ sơ được cập nhật nếu hợp lệ; hệ thống từ chối và giữ giá trị cũ nếu không hợp lệ.

#### Domain Testing - Bước 2: Phân vùng tương đương

| Class ID | Domain Class | Representative Values | Expected Status | Reason |
| :--- | :--- | :--- | :--- | :--- |
| N-V1 | Tên hợp lệ có chữ cái Unicode và khoảng trắng giữa các từ | `Nguyễn Văn An` | Accepted | Có ít nhất một chữ cái thực sự, chỉ dùng ký tự chữ và khoảng trắng hợp lệ |
| N-I1 | Bỏ trống | `""` | Rejected | Trường bắt buộc không được để trống |
| N-I2 | Chỉ toàn khoảng trắng | `"     "` | Rejected | Sau khi trim không còn chữ cái thực sự |
| N-I3 | Chứa chữ số | `Nguyen1` | Rejected | Tên không được chứa số |
| N-I4 | Chứa ký tự đặc biệt | `Nguyen@Van` | Rejected | Tên không được chứa ký tự đặc biệt |
| N-I5 | Chứa emoji | `Nguyen😀` | Rejected | Emoji không thuộc domain hợp lệ |
| N-I6 | Chứa mã script/XSS | `<script>alert(1)</script>` | Rejected | Payload script phải bị từ chối |

#### Domain Testing - Bước 3: Thiết kế test case theo phân vùng

| TC ID | Domain Class | Test Data | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- |
| FR04-N-TC01 | N-V1 | `name = "Nguyễn Văn An"` | Đăng nhập, mở trang Profile, nhập `name`, submit | Profile được cập nhật thành công |
| FR04-N-TC02 | N-I1 | `name = ""` | Đăng nhập, mở trang Profile, để trống `name`, submit | Hệ thống báo lỗi bắt buộc nhập; giá trị cũ không đổi |
| FR04-N-TC03 | N-I2 | `name = "     "` | Đăng nhập, mở trang Profile, nhập `name`, submit | Hệ thống báo lỗi tên không hợp lệ; giá trị cũ không đổi |
| FR04-N-TC04 | N-I3 | `name = "Nguyen1"` | Đăng nhập, mở trang Profile, nhập `name`, submit | Hệ thống báo lỗi tên không được chứa số |
| FR04-N-TC05 | N-I4 | `name = "Nguyen@Van"` | Đăng nhập, mở trang Profile, nhập `name`, submit | Hệ thống báo lỗi tên không được chứa ký tự đặc biệt |
| FR04-N-TC06 | N-I5 | `name = "Nguyen😀"` | Đăng nhập, mở trang Profile, nhập `name`, submit | Hệ thống báo lỗi tên không được chứa emoji |
| FR04-N-TC07 | N-I6 | `name = "<script>alert(1)</script>"` | Đăng nhập, mở trang Profile, nhập `name`, submit | Hệ thống từ chối payload script/XSS |

#### Domain Testing - Bước 4: Ghi nhận bao phủ
- Bao phủ 1 lớp hợp lệ và 6 lớp không hợp lệ.
- Mỗi test case không hợp lệ chỉ nhắm vào một lớp lỗi chính.

#### BVA - Bước 1: Xác định biên
- `MIN = 1`, `MAX = 50`, tính theo số ký tự của `name`.

#### BVA - Bước 2: Chọn giá trị biên

| Boundary Type | Value | Test Data | Expected Status | Reason |
| :--- | :--- | :--- | :--- | :--- |
| Min-1 | 0 | `""` | Rejected | Dưới độ dài tối thiểu |
| Min | 1 | `a` | Accepted | Đúng biên dưới |
| Min+1 | 2 | `aa` | Accepted | Ngay trên biên dưới |
| Nominal | 25 | `aaaaaaaaaaaaaaaaaaaaaaaaa` | Accepted | Giá trị bình thường trong miền |
| Max-1 | 49 | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | Accepted | Ngay dưới biên trên |
| Max | 50 | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | Accepted | Đúng biên trên |
| Max+1 | 51 | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | Rejected | Vượt quá độ dài tối đa |

#### BVA - Bước 3: Thiết kế test case BVA

| TC ID | Boundary Type | Test Data | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- |
| FR04-N-BVA-TC01 | Min-1 | `name = ""` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống từ chối do dưới 1 ký tự |
| FR04-N-BVA-TC02 | Min | `name = "a"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-N-BVA-TC03 | Min+1 | `name = "aa"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-N-BVA-TC04 | Nominal | `name = "aaaaaaaaaaaaaaaaaaaaaaaaa"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-N-BVA-TC05 | Max-1 | `name = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-N-BVA-TC06 | Max | `name = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-N-BVA-TC07 | Max+1 | `name = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống từ chối do vượt quá 50 ký tự |

## 2. Phone

#### Domain Testing - Bước 1: Xác định biến đầu vào & đầu ra
- Input: `phone`
- Output mong đợi: hồ sơ được cập nhật nếu số điện thoại hợp lệ; hệ thống từ chối nếu sai format.

#### Domain Testing - Bước 2: Phân vùng tương đương

| Class ID | Domain Class | Representative Values | Expected Status | Reason |
| :--- | :--- | :--- | :--- | :--- |
| P-V1 | Số Việt Nam bắt đầu bằng `0`, chỉ chứa chữ số | `0912345678` | Accepted | Đúng prefix và chỉ gồm chữ số |
| P-V2 | Số Việt Nam bắt đầu bằng `+84`, phía sau là chữ số | `+84912345678` | Accepted | Đúng prefix quốc tế Việt Nam |
| P-I1 | Bỏ trống | `""` | Rejected | Trường bắt buộc không được để trống |
| P-I2 | Chứa chữ cái | `09123abc78` | Rejected | Số điện thoại không được chứa chữ cái |
| P-I3 | Chứa khoảng trắng/dấu phân tách | `0912 345 678` | Rejected | Format chỉ cho phép chữ số hoặc prefix `+84` |
| P-I4 | Sai tiền tố | `1912345678` | Rejected | Không bắt đầu bằng `0` hoặc `+84` |
| P-I5 | Sai kiểu dữ liệu | `912345678` dạng JSON number | Rejected | API phải nhận phone dưới dạng string |

#### Domain Testing - Bước 3: Thiết kế test case theo phân vùng

| TC ID | Domain Class | Test Data | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- |
| FR04-P-TC01 | P-V1 | `phone = "0912345678"` | Đăng nhập, mở Profile, nhập `phone`, submit | Profile được cập nhật thành công |
| FR04-P-TC02 | P-V2 | `phone = "+84912345678"` | Đăng nhập, mở Profile, nhập `phone`, submit | Profile được cập nhật thành công |
| FR04-P-TC03 | P-I1 | `phone = ""` | Đăng nhập, mở Profile, để trống `phone`, submit | Hệ thống báo lỗi bắt buộc nhập |
| FR04-P-TC04 | P-I2 | `phone = "09123abc78"` | Đăng nhập, mở Profile, nhập `phone`, submit | Hệ thống báo lỗi phone chỉ được chứa chữ số hoặc prefix hợp lệ |
| FR04-P-TC05 | P-I3 | `phone = "0912 345 678"` | Đăng nhập, mở Profile, nhập `phone`, submit | Hệ thống báo lỗi format phone không hợp lệ |
| FR04-P-TC06 | P-I4 | `phone = "1912345678"` | Đăng nhập, mở Profile, nhập `phone`, submit | Hệ thống báo lỗi sai prefix |
| FR04-P-TC07 | P-I5 | `phone = 912345678` | Gửi request cập nhật Profile với `phone` là number, không phải string | Hệ thống từ chối kiểu dữ liệu không hợp lệ |

#### Domain Testing - Bước 4: Ghi nhận bao phủ
- Bao phủ 2 lớp hợp lệ và 5 lớp không hợp lệ.
- Mỗi test case không hợp lệ chỉ nhắm vào một lớp lỗi chính.

#### BVA - Bước 1: Xác định biên
- Với số bắt đầu bằng `0`: `MIN = 10`, `MAX = 11`, tính theo số chữ số.

#### BVA - Bước 2: Chọn giá trị biên

| Boundary Type | Value | Test Data | Expected Status | Reason |
| :--- | :--- | :--- | :--- | :--- |
| Min-1 | 9 | `091234567` | Rejected | Dưới 10 chữ số |
| Min | 10 | `0912345678` | Accepted | Đúng biên dưới |
| Min+1 | 11 | `01234567890` | Accepted | Ngay trên biên dưới và cũng là biên trên |
| Nominal | 10 | `0987654321` | Accepted | Giá trị hợp lệ điển hình vì miền chỉ có 10 hoặc 11 chữ số |
| Max-1 | 10 | `0912345678` | Accepted | Ngay dưới biên trên |
| Max | 11 | `01234567890` | Accepted | Đúng biên trên |
| Max+1 | 12 | `012345678901` | Rejected | Vượt quá 11 chữ số |

#### BVA - Bước 3: Thiết kế test case BVA

| TC ID | Boundary Type | Test Data | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- |
| FR04-P-BVA-TC01 | Min-1 | `phone = "091234567"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống từ chối do dưới 10 chữ số |
| FR04-P-BVA-TC02 | Min | `phone = "0912345678"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-P-BVA-TC03 | Min+1 | `phone = "01234567890"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-P-BVA-TC04 | Nominal | `phone = "0987654321"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-P-BVA-TC05 | Max-1 | `phone = "0912345678"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-P-BVA-TC06 | Max | `phone = "01234567890"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-P-BVA-TC07 | Max+1 | `phone = "012345678901"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống từ chối do vượt quá 11 chữ số |

### 3. Address

#### Domain Testing - Bước 1: Xác định biến đầu vào & đầu ra
- Input: `address`
- Output mong đợi: hồ sơ được cập nhật nếu địa chỉ hợp lệ; hệ thống từ chối nếu địa chỉ rỗng hoặc chứa ký tự/payload không hợp lệ.

#### Domain Testing - Bước 2: Phân vùng tương đương

| Class ID | Domain Class | Representative Values | Expected Status | Reason |
| :--- | :--- | :--- | :--- | :--- |
| A-V1 | Địa chỉ hợp lệ có Unicode, chữ số, khoảng trắng, dấu câu cơ bản | `123 Nguyễn Trãi, P.2-Q.5` | Accepted | Thuộc tập ký tự được hỗ trợ |
| A-I1 | Bỏ trống | `""` | Rejected | Trường bắt buộc không được để trống |
| A-I2 | Chỉ toàn khoảng trắng | `"     "` | Rejected | Sau khi trim không còn nội dung |
| A-I3 | Chứa HTML/script | `<script>alert(1)</script>` | Rejected | Payload HTML/script phải bị từ chối |
| A-I4 | Chứa emoji | `12 Nguyễn Trãi😀` | Rejected | Emoji không thuộc domain hợp lệ |
| A-I5 | Chứa ký tự đặc biệt không hỗ trợ | `12 Nguyễn Trãi ~ Q.5` | Rejected | Ký tự `~` không nằm trong dấu câu cơ bản được chấp nhận |

#### Domain Testing - Bước 3: Thiết kế test case theo phân vùng

| TC ID | Domain Class | Test Data | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- |
| FR04-A-TC01 | A-V1 | `address = "123 Nguyễn Trãi, P.2-Q.5"` | Đăng nhập, mở Profile, nhập `address`, submit | Profile được cập nhật thành công |
| FR04-A-TC02 | A-I1 | `address = ""` | Đăng nhập, mở Profile, để trống `address`, submit | Hệ thống báo lỗi bắt buộc nhập |
| FR04-A-TC03 | A-I2 | `address = "     "` | Đăng nhập, mở Profile, nhập `address`, submit | Hệ thống báo lỗi địa chỉ không hợp lệ |
| FR04-A-TC04 | A-I3 | `address = "<script>alert(1)</script>"` | Đăng nhập, mở Profile, nhập `address`, submit | Hệ thống từ chối payload HTML/script |
| FR04-A-TC05 | A-I4 | `address = "12 Nguyễn Trãi😀"` | Đăng nhập, mở Profile, nhập `address`, submit | Hệ thống báo lỗi địa chỉ không được chứa emoji |
| FR04-A-TC06 | A-I5 | `address = "12 Nguyễn Trãi ~ Q.5"` | Đăng nhập, mở Profile, nhập `address`, submit | Hệ thống báo lỗi ký tự không hỗ trợ |

#### Domain Testing - Bước 4: Ghi nhận bao phủ
- Bao phủ 1 lớp hợp lệ và 5 lớp không hợp lệ.
- Mỗi test case không hợp lệ chỉ nhắm vào một lớp lỗi chính.

#### BVA - Bước 1: Xác định biên
- `MIN = 5`, `MAX = 255`, tính theo số ký tự sau khi trim.

#### BVA - Bước 2: Chọn giá trị biên

| Boundary Type | Value | Test Data | Expected Status | Reason |
| :--- | :--- | :--- | :--- | :--- |
| Min-1 | 4 | `1234` | Rejected | Dưới 5 ký tự sau trim |
| Min | 5 | `12345` | Accepted | Đúng biên dưới |
| Min+1 | 6 | `123456` | Accepted | Ngay trên biên dưới |
| Nominal | 130 | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | Accepted | Giá trị bình thường trong miền |
| Max-1 | 254 | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | Accepted | Ngay dưới biên trên |
| Max | 255 | `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa` | Accepted | Đúng biên trên |
| Max+1 | 256 | `` | Rejected | Vượt quá 255 ký tự sau trim |

#### BVA - Bước 3: Thiết kế test case BVA

| TC ID | Boundary Type | Test Data | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- |
| FR04-A-BVA-TC01 | Min-1 | `address = "1234"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống từ chối do dưới 5 ký tự sau trim |
| FR04-A-BVA-TC02 | Min | `address = "12345"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-A-BVA-TC03 | Min+1 | `address = "123456"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-A-BVA-TC04 | Nominal | `address = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-A-BVA-TC05 | Max-1 | `address = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-A-BVA-TC06 | Max | `address = "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống chấp nhận |
| FR04-A-BVA-TC07 | Max+1 | `address = ""` | Đăng nhập, mở Profile, nhập dữ liệu biên, submit | Hệ thống từ chối do vượt quá 255 ký tự sau trim |

### AI Audit Extract

```markdown
Name of the AI tool: GPT-5 Codex
Date and time: 2026-06-26 07:11:41 +07
Your prompt: Áp dụng kỹ năng# Domain Testing & BVA trong SKILL.md để thiết kế test case cho FR-04 Personal profile management với 3 input Name, Phone, Address; xuất Markdown theo template, và thêm AI Audit Extract.
The AI output: Toàn bộ bảng phân vùng tương đương, test case# Domain Testing, bảng giá trị biên, test case BVA cho Name/Phone/Address ở trên.
```

---

## FR-10: Order State Machine - Domain Testing & BVA

### Bước 1: Xác định biến Đầu vào & Đầu ra

| Variable | Type | Description |
| :--- | :--- | :--- |
| Current State | Input | Trạng thái hiện tại của đơn hàng: `pending`, `confirmed`, `shipping`, `delivered`, `canceled` |
| Action/Event | Input | Hành động kích hoạt: `Confirm`, `Cancel`, `Ship`, `Deliver` |
| Pending Age Hours | Input for BVA | Số giờ đơn hàng ở trạng thái `pending`, MIN = 0, MAX = 24 |
| Next State / Error Message | Output | Trạng thái mới của đơn hàng hoặc thông báo lỗi từ chối thao tác |

Giả định: với transition không hợp lệ, hệ thống từ chối thao tác và trả lỗi phù hợp như `Invalid order state transition`; với state/action không tồn tại, hệ thống trả lỗi như `Invalid order status/action`.

### Bước 2: Phân tích Phân vùng tương đương

| Class ID | Domain Class | Representative Values | Expected Status | Reason |
| :--- | :--- | :--- | :--- | :--- |
| S-V01 | Valid transition | `pending` + `Confirm` -> `confirmed` | Accepted | Luồng xác nhận đơn hàng hợp lệ |
| S-V02 | Valid transition | `pending` + `Cancel` -> `canceled` | Accepted | Đơn hàng chờ xử lý được phép hủy |
| S-V03 | Valid transition | `confirmed` + `Ship` -> `shipping` | Accepted | Đơn hàng đã xác nhận được phép bắt đầu giao |
| S-V04 | Valid transition | `confirmed` + `Cancel` -> `canceled` | Accepted | Đơn hàng đã xác nhận vẫn được phép hủy theo giả định |
| S-V05 | Valid transition | `shipping` + `Deliver` -> `delivered` | Accepted | Đơn hàng đang giao được phép chuyển sang đã giao |
| S-I01 | Invalid transition | `shipping` + `Cancel` | Rejected | Không được hủy khi đơn hàng đang giao |
| S-I02 | Invalid transition | `delivered` + `Cancel` | Rejected | Không được hủy khi đơn hàng đã giao |
| S-I03 | Invalid transition from final state | `canceled` + `Confirm` | Rejected | `canceled` là final state |
| S-I04 | Invalid transition from final state | `canceled` + `Ship` | Rejected | `canceled` là final state |
| S-I05 | Invalid transition from final state | `canceled` + `Deliver` | Rejected | `canceled` là final state |
| S-I06 | Invalid transition from final state | `delivered` + `Confirm` | Rejected | `delivered` là final state |
| S-I07 | Invalid transition from final state | `delivered` + `Ship` | Rejected | `delivered` là final state |
| S-I08 | Invalid transition from final state | `delivered` + `Deliver` | Rejected | Không được chuyển tiếp khi đã ở `delivered` |
| S-I09 | Invalid jump transition | `pending` + `Ship` | Rejected | Không được nhảy từ `pending` sang `shipping` |
| S-I10 | Invalid jump transition | `pending` + `Deliver` | Rejected | Không được nhảy từ `pending` sang `delivered` |
| S-I11 | Invalid jump transition | `confirmed` + `Deliver` | Rejected | Không được bỏ qua trạng thái `shipping` |
| S-I12 | Invalid action | `pending` + `Refund` | Rejected | `Refund` không tồn tại trong state machine |
| S-I13 | Invalid state | `returned` + `Confirm` | Rejected | `returned` không tồn tại trong state machine |

### Bước 3: Phân tích Giá trị biên

| Boundary Type | Value | Test Data | Expected Status | Reason |
| :--- | :--- | :--- | :--- | :--- |
| Min-1 | -1 | `pending_age_hours = -1` | Rejected | Thời gian âm là dữ liệu không hợp lệ |
| Min | 0 | `pending_age_hours = 0` | Accepted | Vừa tạo đơn hàng, chưa quá hạn |
| Min+1 | 1 | `pending_age_hours = 1` | Accepted | Vẫn trong thời hạn chờ 24 giờ |
| Nominal | 12 | `pending_age_hours = 12` | Accepted | Giá trị bình thường giữa 0 và 24 |
| Max-1 | 23 | `pending_age_hours = 23` | Accepted | Ngay dưới giới hạn tối đa |
| Max | 24 | `pending_age_hours = 24` | Accepted | Đúng giới hạn tối đa, chưa quá 24 giờ |
| Max+1 | 25 | `pending_age_hours = 25` | Rejected | Quá 24 giờ, hệ thống tự động chuyển sang `canceled` |

### Bước 4: Thiết kế Test Cases & Ghi nhận

| TC ID | Domain Class | Test Data | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- |
| FR10-S-TC01 | S-V01 | Current State = `pending`; Action = `Confirm` | Tạo đơn hàng `pending`; thực hiện `Confirm`; tải lại đơn hàng | Trạng thái mới là `confirmed` |
| FR10-S-TC02 | S-V02 | Current State = `pending`; Action = `Cancel` | Tạo đơn hàng `pending`; thực hiện `Cancel`; tải lại đơn hàng | Trạng thái mới là `canceled` |
| FR10-S-TC03 | S-V03 | Current State = `confirmed`; Action = `Ship` | Tạo đơn hàng `confirmed`; thực hiện `Ship`; tải lại đơn hàng | Trạng thái mới là `shipping` |
| FR10-S-TC04 | S-V04 | Current State = `confirmed`; Action = `Cancel` | Tạo đơn hàng `confirmed`; thực hiện `Cancel`; tải lại đơn hàng | Trạng thái mới là `canceled` |
| FR10-S-TC05 | S-V05 | Current State = `shipping`; Action = `Deliver` | Tạo đơn hàng `shipping`; thực hiện `Deliver`; tải lại đơn hàng | Trạng thái mới là `delivered` |
| FR10-S-TC06 | S-I01 | Current State = `shipping`; Action = `Cancel` | Tạo đơn hàng `shipping`; thực hiện `Cancel`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `shipping` |
| FR10-S-TC07 | S-I02 | Current State = `delivered`; Action = `Cancel` | Tạo đơn hàng `delivered`; thực hiện `Cancel`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `delivered` |
| FR10-S-TC08 | S-I03 | Current State = `canceled`; Action = `Confirm` | Tạo đơn hàng `canceled`; thực hiện `Confirm`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `canceled` |
| FR10-S-TC09 | S-I04 | Current State = `canceled`; Action = `Ship` | Tạo đơn hàng `canceled`; thực hiện `Ship`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `canceled` |
| FR10-S-TC10 | S-I05 | Current State = `canceled`; Action = `Deliver` | Tạo đơn hàng `canceled`; thực hiện `Deliver`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `canceled` |
| FR10-S-TC11 | S-I06 | Current State = `delivered`; Action = `Confirm` | Tạo đơn hàng `delivered`; thực hiện `Confirm`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `delivered` |
| FR10-S-TC12 | S-I07 | Current State = `delivered`; Action = `Ship` | Tạo đơn hàng `delivered`; thực hiện `Ship`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `delivered` |
| FR10-S-TC13 | S-I08 | Current State = `delivered`; Action = `Deliver` | Tạo đơn hàng `delivered`; thực hiện `Deliver`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `delivered` |
| FR10-S-TC14 | S-I09 | Current State = `pending`; Action = `Ship` | Tạo đơn hàng `pending`; thực hiện `Ship`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `pending` |
| FR10-S-TC15 | S-I10 | Current State = `pending`; Action = `Deliver` | Tạo đơn hàng `pending`; thực hiện `Deliver`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `pending` |
| FR10-S-TC16 | S-I11 | Current State = `confirmed`; Action = `Deliver` | Tạo đơn hàng `confirmed`; thực hiện `Deliver`; tải lại đơn hàng | Hệ thống từ chối; trạng thái vẫn là `confirmed` |
| FR10-S-TC17 | S-I12 | Current State = `pending`; Action = `Refund` | Tạo đơn hàng `pending`; gửi action `Refund`; tải lại đơn hàng | Hệ thống từ chối do action không tồn tại; trạng thái vẫn là `pending` |
| FR10-S-TC18 | S-I13 | Current State = `returned`; Action = `Confirm` | Gửi request với current state không hợp lệ `returned`; thực hiện `Confirm` | Hệ thống từ chối do state không tồn tại |

<br>

| TC ID | Boundary Type | Test Data | Steps | Expected Result |
| :--- | :--- | :--- | :--- | :--- |
| FR10-T-BVA-TC01 | Min-1 | Current State = `pending`; `pending_age_hours = -1` | Thiết lập thời gian pending âm; kích hoạt kiểm tra timeout | Hệ thống từ chối dữ liệu thời gian không hợp lệ |
| FR10-T-BVA-TC02 | Min | Current State = `pending`; `pending_age_hours = 0`; Action = `Confirm` | Tạo đơn hàng vừa vào `pending`; thực hiện `Confirm` | Hệ thống chấp nhận; trạng thái mới là `confirmed` |
| FR10-T-BVA-TC03 | Min+1 | Current State = `pending`; `pending_age_hours = 1`; Action = `Confirm` | Tạo đơn hàng pending 1 giờ; thực hiện `Confirm` | Hệ thống chấp nhận; trạng thái mới là `confirmed` |
| FR10-T-BVA-TC04 | Nominal | Current State = `pending`; `pending_age_hours = 12`; Action = `Confirm` | Tạo đơn hàng pending 12 giờ; thực hiện `Confirm` | Hệ thống chấp nhận; trạng thái mới là `confirmed` |
| FR10-T-BVA-TC05 | Max-1 | Current State = `pending`; `pending_age_hours = 23`; Action = `Confirm` | Tạo đơn hàng pending 23 giờ; thực hiện `Confirm` | Hệ thống chấp nhận; trạng thái mới là `confirmed` |
| FR10-T-BVA-TC06 | Max | Current State = `pending`; `pending_age_hours = 24`; Action = `Confirm` | Tạo đơn hàng pending đúng 24 giờ; thực hiện `Confirm` | Hệ thống chấp nhận vì chưa quá 24 giờ; trạng thái mới là `confirmed` |
| FR10-T-BVA-TC07 | Max+1 | Current State = `pending`; `pending_age_hours = 25`; Action = `Confirm` | Tạo đơn hàng pending 25 giờ; kích hoạt kiểm tra timeout rồi thực hiện `Confirm` | Hệ thống tự động chuyển sang `canceled`; thao tác `Confirm` bị từ chối |

### AI Audit Extract

```markdown
Name of the AI tool: Codex (GPT-5)
Date and time: 2026-06-26 07:26:11 +0700
Your prompt: Áp dụng kỹ năng Domain Testing & BVA trong SKILL.md để thiết kế test case cho FR-10: Order state machine, gồm 4 bước Domain Testing, bảng Markdown theo template, mỗi invalid partition có test case riêng, và sinh AI Audit Extract.
The AI output: Toàn bộ bảng phân vùng tương đương, bảng BVA, domain test cases và BVA test cases cho FR-10 được sinh ra ở trên.
