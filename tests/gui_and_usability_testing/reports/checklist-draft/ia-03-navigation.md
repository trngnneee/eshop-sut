# Checklist Draft — IA-03: Navigation

- **Input:** inventory 9 file trong `reports/23127438/ui-inventory/` + FR-23 nguyên văn (`eshop-sut/README.md:258-263`)
- **FR-23 gồm 4 quy tắc:** (1) Navbar highlight trang đang chọn; (2) link "Giỏ hàng" có badge số lượng; (3) nút đăng xuất nhãn "Đăng xuất" (không phải "Thoát"); (4) breadcrumb bắt buộc ở trang con (Giỏ hàng, Thanh toán, Chi tiết sản phẩm).

| ID | Screen(s) | Checklist Item | Expected Result | Traced to (FR-23 / heuristic) |
|---|---|---|---|---|
| GUI-IA03-01 | Tất cả 8 màn hình (Header) | Navbar highlight mục đang được chọn: đứng ở `/cart` thì link "Giỏ hàng" có style active khác các link còn lại (inventory: các link chỉ có `hover:underline`, không có active state — App.jsx:22-37) | Link tương ứng trang hiện tại được highlight (đậm/gạch chân/đổi màu) | FR-23 (active highlight) |
| GUI-IA03-02 | Tất cả 8 màn hình (Header) | Link "Giỏ hàng" hiển thị badge số lượng sản phẩm, cập nhật ngay khi bấm "Thêm vào giỏ" (inventory: link trần không có badge — App.jsx:23) | Badge hiện đúng số item; thêm 1 SP → badge +1 tức thì | FR-23 (badge/counter) |
| GUI-IA03-03 | Tất cả 8 màn hình (Header, đã đăng nhập) | Nút đăng xuất có nhãn chính xác "Đăng xuất" (inventory: nút ghi **"Thoát"** — App.jsx:29) | Nhãn đúng từng chữ: "Đăng xuất" | FR-23 (exact label wording) |
| GUI-IA03-04 | Chi tiết SP, Giỏ hàng, Thanh toán | Có breadcrumb ở 3 trang con theo spec, vd "Trang chủ > Giỏ hàng > Thanh toán" (inventory: không có breadcrumb nào trong code cả 3 trang) | Breadcrumb hiện đúng cấp, click được để quay lại | FR-23 (breadcrumb) |
| GUI-IA03-05 | Toàn app | Truy cập URL không tồn tại (vd `/abc`, `/admin`) hiển thị trang 404 thân thiện (inventory: không có route catch-all — App.jsx:50-59 → render trang trắng trong layout) | Trang 404 có message + link về trang chủ, không phải trang trắng | Heuristic (invalid-URL/404) |
| GUI-IA03-06 | Chi tiết SP | Truy cập `/product/999` (id không tồn tại — runtime xác nhận API trả `{}`) hiển thị thông báo thân thiện kèm đường quay về (inventory: hiện text kỹ thuật "Sản phẩm không tồn tại (Lỗi trắng trang do data rỗng)", không có link — ProductDetail.jsx:35-36) | Message thân thiện + link/nút về trang chủ | Heuristic (not-found handling) |
| GUI-IA03-07 | Đăng nhập | Link "Quên mật khẩu?" điều hướng SPA không reload cả trang (inventory: dùng thẻ `<a href>` thay vì `<Link>` → full page reload — Login.jsx:49-51) | Chuyển trang mượt như các link khác, không trắng màn hình | Heuristic (navigation consistency) |
| GUI-IA03-08 | Thanh toán | Trang Thanh toán có link/nút quay lại Giỏ hàng để sửa đơn trước khi xác nhận (inventory: không có back link nào trong form — Checkout.jsx:79-150; Giỏ hàng thì có "← Mua tiếp" — Cart.jsx:66-68) | Có đường quay lại giỏ hàng, không mất dữ liệu | Heuristic (back/continue links) |
| GUI-IA03-09 | Giỏ hàng → Đăng nhập | Flow bị chặn checkout vì chưa đăng nhập: sau khi đăng nhập thành công phải quay lại giỏ hàng/checkout (inventory: Cart redirect `/login` — Cart.jsx:14; Login thành công luôn về `/` — Login.jsx:16) | Đăng nhập xong quay về đúng ngữ cảnh trước đó | Heuristic (redirect flow) |
| GUI-IA03-10 | Thanh toán | Sau khi thanh toán thành công, bấm Back của trình duyệt không quay lại form checkout ở trạng thái có thể submit lại đơn cũ (state `success` — Checkout.jsx:69-77) | Back không cho phép re-submit đơn | Heuristic (browser back-button) |
| GUI-IA03-11 | Quên mật khẩu | Đang ở bước 2 (OTP), bấm Back của trình duyệt: vì bước không gắn vào URL/history (state `step` — ForgotPassword.jsx:8), Back sẽ rời hẳn trang và mất tiến trình | Back quay về bước 1 (hoặc giữ được tiến trình), không mất OTP đã lấy | Heuristic (browser back-button, multi-step) |
| GUI-IA03-12 | Thanh toán | Vào thẳng `/checkout` bằng URL khi giỏ trống hoặc chưa đăng nhập bị chặn/chuyển hướng (inventory: không có guard nào — Checkout.jsx; giỏ rỗng vẫn hiện form tổng 0 ₫) | Giỏ trống → redirect về giỏ hàng; chưa login → redirect đăng nhập | Heuristic (route guarding) |
| GUI-IA03-13 | Hồ sơ/Đơn hàng | Truy cập `/profile` khi chưa đăng nhập: thông báo có kèm link/nút tới trang đăng nhập (inventory: chỉ có text trần "Vui lòng đăng nhập" — Profile.jsx:109) | Có link "Đăng nhập" hoặc tự redirect | Heuristic (dead-end navigation) |
| GUI-IA03-14 | Tất cả 8 màn hình | Logo "EShop" ở header luôn điều hướng về trang chủ từ mọi màn hình (App.jsx:21) | Click logo → về `/` từ bất kỳ trang nào | Heuristic (logo home link) — kiểm chứng pass |
| GUI-IA03-15 | Trang chủ, Lịch sử ĐH | Danh sách dài có phân trang/lazy-load hoặc hiển thị đầy đủ không vỡ layout (inventory: cả grid sản phẩm — Home.jsx:75 — và bảng đơn hàng — Profile.jsx:172-213 — đều render toàn bộ, không phân trang) | Danh sách nhiều item vẫn dùng được, không vỡ bảng | Heuristic (pagination if present) |

**Coverage FR-23:** active highlight (01) · badge giỏ hàng (02) · nhãn "Đăng xuất" (03) · breadcrumb (04) · ngoài FR-23: 404/not-found (05-06), SPA/back-button (07, 10, 11), back/continue (08-09), guard (12-13), khác (14-15).
