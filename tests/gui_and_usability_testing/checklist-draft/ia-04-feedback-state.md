# Checklist Draft — IA-04: Feedback & State

- **Input:** inventory 9 file trong `../ui-inventory/` + FR-24 nguyên văn (`eshop-sut/README.md:265-270`)
- **FR-24 gồm 4 quy tắc:** (1) "Thêm vào giỏ" phải có phản hồi trực quan (toast/badge); (2) xoá item khỏi giỏ phải có dialog xác nhận; (3) empty state phải có icon/hình minh hoạ + message thân thiện; (4) mọi ảnh sản phẩm có `alt` mô tả (không rỗng).

| ID | Screen(s) | Checklist Item | Expected Result | Traced to (FR-24 / heuristic) |
|---|---|---|---|---|
| GUI-IA04-01 | Trang chủ | Bấm "Thêm vào giỏ" trên card sản phẩm có phản hồi trực quan ngay (inventory: không có bất kỳ feedback nào — Home.jsx:98-103; header cũng không có badge) | Toast/badge xuất hiện ngay sau click | FR-24 (add-to-cart feedback) |
| GUI-IA04-02 | Chi tiết SP | MỖI lần bấm "Thêm vào giỏ hàng" đều thêm sản phẩm và có feedback ngay từ lần bấm đầu tiên (inventory: click đầu bị nuốt bởi biến `clickCount` — ProductDetail.jsx:22-32; feedback "Đã thêm" 2s chỉ hiện từ click thứ 2) | Click 1 lần → vào giỏ ngay + feedback "Đã thêm" | FR-24 (add-to-cart feedback) |
| GUI-IA04-03 | Giỏ hàng | Bấm "Xóa" item hiển thị dialog xác nhận trước khi xoá (inventory: xoá ngay không hỏi — Cart.jsx:50-55) | Dialog "Bạn có chắc muốn xóa?" 2 lựa chọn; Hủy → giữ nguyên item | FR-24 (confirmation dialog) |
| GUI-IA04-04 | Lịch sử ĐH | Bấm "Hủy đơn" hiển thị dialog xác nhận trước khi huỷ (inventory: huỷ ngay không hỏi — Profile.jsx:200-208; nút còn hiện cả khi đơn "Đang giao" — comment code :200) | Dialog xác nhận trước hành động không hoàn tác được | FR-24 (confirmation dialog) |
| GUI-IA04-05 | Giỏ hàng, Lịch sử ĐH | Empty state có icon/hình minh hoạ kèm message thân thiện (inventory: Giỏ hàng chỉ có text "Giỏ hàng của bạn đang trống" — Cart.jsx:20-27; Đơn hàng chỉ có text "Bạn chưa có đơn hàng nào." — Profile.jsx:169-170) | Có icon/hình + message + CTA | FR-24 (empty-state visuals) |
| GUI-IA04-06 | Trang chủ | Tìm kiếm 0 kết quả hiển thị empty state (inventory: KHÔNG có gì — grid trống, dòng đếm cũng ẩn vì điều kiện `products.length > 0` — Home.jsx:75-114) | Message "Không tìm thấy sản phẩm cho '<từ khoá>'" + icon | FR-24 (empty-state visuals) |
| GUI-IA04-07 | Trang chủ | Mọi ảnh sản phẩm có `alt` mô tả nội dung (inventory: card dùng `alt=""` — Home.jsx:81-85; Chi tiết SP đã đạt với `alt={product.name}` — ProductDetail.jsx:41-45) | `alt` = tên sản phẩm, không rỗng | FR-24 (image alt-text) |
| GUI-IA04-08 | Trang chủ, Lịch sử ĐH, Chi tiết SP | Thao tác tải dữ liệu có loading indicator (inventory: Home fetch không indicator — Home.jsx:13-30; danh sách đơn không indicator — Profile.jsx:15-30; Chi tiết SP chỉ có plain text "Đang tải..." — ProductDetail.jsx:34); giả lập Slow 3G bằng DevTools để quan sát | Spinner/skeleton trong lúc chờ | Heuristic (loading indicators) |
| GUI-IA04-09 | Chi tiết SP | Khi API lỗi/backend chết, trang hiển thị error state thay vì kẹt vô hạn ở "Đang tải..." (inventory: fetch lỗi chỉ `console.error` — ProductDetail.jsx:15-20) | Message lỗi + nút thử lại/về trang chủ | Heuristic (error state) |
| GUI-IA04-10 | Quên MK, Hồ sơ, Giỏ hàng, Thanh toán | Feedback thành công/lỗi dùng UI trong trang (toast/alert-box) nhất quán, không dùng `alert()` native (inventory: 8+ chỗ dùng `alert()` — ForgotPassword.jsx:21,29,35,38; Profile.jsx:45,61,63,76,79; Cart.jsx:13; Checkout.jsx:64) | Toàn app 1 pattern feedback thống nhất trong trang | Heuristic (toast consistency) |
| GUI-IA04-11 | Đăng nhập | Sau 3 lần đăng nhập sai, UI thông báo rõ tài khoản bị khoá 30 giây (theo FR-02) thay vì message chung (inventory: mọi lỗi đều hiện "Đăng nhập thất bại. Vui lòng kiểm tra lại." — Login.jsx:17-19) | Message phân biệt: sai mật khẩu vs tài khoản đang khoá (kèm thời gian) | FR-24 + FR-02 (account-lockout messaging) |
| GUI-IA04-12 | Thanh toán | Feedback coupon đủ 2 nhánh: mã hợp lệ → ✅ message + tiền tiết kiệm + thành tiền; mã sai → message lỗi đỏ (inventory: đã có cả 2 — Checkout.jsx:125-134); kiểm tra thêm số tiền hiển thị tính đúng | Cả 2 nhánh hiển thị đúng, số tiền chính xác | FR-24 (coupon apply/invalid feedback) — kiểm chứng pass |
| GUI-IA04-13 | Trang chủ | Từ khoá tìm kiếm hiển thị lại được render an toàn: nhập `<script>alert(1)</script>` và `<img src=x onerror=alert(1)>` (inventory: echo từ khoá bằng `dangerouslySetInnerHTML` — Home.jsx:62-67) | Từ khoá hiện dạng text thuần, không thực thi HTML/JS | FR-24 + README mục 1 (safe rendering: search terms) |
| GUI-IA04-14 | Header, Hồ sơ | Tên user và địa chỉ giao hàng render an toàn: đăng ký tên chứa `<b>x</b>`/`<script>` rồi xem header "Chào, {name}" (inventory: render bằng `dangerouslySetInnerHTML` — App.jsx:26-28); lưu địa chỉ chứa HTML rồi reload trang Hồ sơ | Text thuần, không thực thi HTML | FR-24 + README mục 1 (safe rendering: user text) |
| GUI-IA04-15 | Trang chủ | Lỗi backend hiển thị dạng thân thiện, không lộ nội dung kỹ thuật (runtime xác nhận: search với `'` → backend trả raw HTML "Database Error / SQLITE_ERROR" và được render nguyên khối — Home.jsx:19-21, 69-73) | Message kiểu "Có lỗi xảy ra, thử lại sau", không lộ SQL/stack | Heuristic (error feedback) |
| GUI-IA04-16 | Thanh toán, Giỏ hàng | Sau thanh toán thành công, giỏ hàng được reset về trống (inventory: `clearCart` được import nhưng không bao giờ gọi — Checkout.jsx:9, 62; quay về trang chủ giỏ vẫn nguyên) | Giỏ trống sau khi đặt hàng thành công | Heuristic (state consistency) |
| GUI-IA04-17 | Lịch sử ĐH | Lỗi API tải đơn hàng hiển thị khác với trạng thái "chưa có đơn" (inventory: fetch lỗi bị nuốt → hiện "Bạn chưa có đơn hàng nào." kể cả khi lỗi — Profile.jsx:26-29) | Lỗi API → message lỗi; empty state chỉ khi thật sự 0 đơn | Heuristic (error vs empty) |
| GUI-IA04-18 | Đăng ký | Đăng ký thành công có thông báo xác nhận trước/khi chuyển sang trang đăng nhập (inventory: navigate thẳng `/login` không message nào — Register.jsx:25) | Toast/message "Đăng ký thành công, mời đăng nhập" | Heuristic (action feedback) |

**Coverage FR-24:** feedback thêm giỏ (01-02) · confirmation dialog (03-04) · empty state (05-06) · image alt (07) · ngoài FR-24 theo aspect list của prompt: loading (08), error state (09, 15, 17), toast consistency (10), lockout (11), coupon (12), safe rendering (13-14), state (16), success feedback (18).
