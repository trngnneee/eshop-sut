# Checklist Draft — IA-02: Forms

- **Input:** inventory 9 file trong `../ui-inventory/` + FR-22 nguyên văn (`eshop-sut/README.md:250-256`)
- **FR-22 gồm 5 quy tắc:** (1) field bắt buộc có dấu `*` cạnh nhãn; (2) field Email dùng `type="email"`; (3) field Mật khẩu dùng `type="password"`; (4) thông báo lỗi hiện **TRÊN** nút submit — **quy tắc ngược convention phổ biến (đặt dưới field), test theo đúng spec này, không "sửa" theo thói quen**; (5) form ≥2 bước phải có Step Indicator.

| ID | Screen(s) | Checklist Item | Expected Result | Traced to (FR-22 / heuristic) |
|---|---|---|---|---|
| GUI-IA02-01 | Đăng ký, Đăng nhập, Quên MK, Hồ sơ | Mọi field có thuộc tính `required` hiển thị dấu `*` cạnh nhãn (inventory: không field nào có `*` — Register.jsx:37-64, Login.jsx:28-45, ForgotPassword.jsx:49-87, Profile.jsx:128-135) | Tất cả field bắt buộc có `*` cạnh nhãn | FR-22 (required indicator) |
| GUI-IA02-02 | Đăng ký, Đăng nhập, Quên MK | Field Email dùng `type="email"` (inventory: cả 3 màn đều dùng `type="text"` — Register.jsx:48-54, Login.jsx:29-35, ForgotPassword.jsx:50-56); nhập "abc" không phải email phải bị chặn | Cả 3 field email có `type="email"`, chặn định dạng sai | FR-22 (input type) |
| GUI-IA02-03 | Đăng nhập | Field Mật khẩu che ký tự khi gõ (inventory: `type="text"` — Login.jsx:39-45; Đăng ký :58-64 và Quên MK :81-87 đã dùng đúng `type="password"`) | Mật khẩu hiển thị dạng chấm tròn trên mọi form | FR-22 (password masking) |
| GUI-IA02-04 | Đăng nhập, Đăng ký | Thông báo lỗi của form xuất hiện **phía TRÊN** nút submit theo đúng spec (inventory: Đăng nhập render lỗi DƯỚI form — Login.jsx:66; Đăng ký đã đúng, lỗi hiện trên form — Register.jsx:34). Lưu ý: spec cố tình ngược convention — item này chấm theo spec, không theo thói quen "lỗi dưới field" | Mọi error message nằm trên nút submit | FR-22 (message placement — anti-convention rule) |
| GUI-IA02-05 | Quên mật khẩu | Luồng 2 bước (email → OTP + mật khẩu mới) có Step Indicator rõ ràng (inventory: không có indicator nào — ForgotPassword.jsx:46-98) | Hiển thị "Bước 1/2", "Bước 2/2" hoặc tương đương | FR-22 (step indicator) |
| GUI-IA02-06 | Quên MK, Hồ sơ | Lỗi validate hiển thị trong trang (trên nút submit) thay vì popup native (inventory: Quên MK validate mật khẩu bằng `alert()` — ForgotPassword.jsx:27-31; Hồ sơ validate SĐT bằng `alert()` — Profile.jsx:44-47) | Lỗi validate là message trong trang, đặt trên nút submit theo FR-22 | FR-22 (placement) + heuristic |
| GUI-IA02-07 | Hồ sơ | Field Số điện thoại chấp nhận số VN chuẩn 10 số bắt đầu bằng 0 (inventory: regex `/^[1-9][0-9]{8,9}$/` từ chối số bắt đầu 0 — Profile.jsx:44 — mâu thuẫn chính placeholder "VD: 0912345678" — :144) | Nhập "0912345678" → hợp lệ; nhập chữ/quá ngắn → bị chặn kèm message rõ | FR-22 (format constraints: phone) |
| GUI-IA02-08 | Đăng ký, Quên MK | Quy tắc mật khẩu thực thi khớp với hint text: nhập "Abcdef1!" (đủ 8 ký tự, hoa, thường, số, ký tự đặc biệt đúng như hint) phải được chấp nhận (inventory: regex thực tế yêu cầu **khoảng trắng** và cấm ký tự đặc biệt — Register.jsx:16-19, ForgotPassword.jsx:27-30; hint tại Register.jsx:65-67) | Mật khẩu đúng như hint mô tả → pass validate; validate và hint không mâu thuẫn | FR-22 (validation) + heuristic |
| GUI-IA02-09 | Quên mật khẩu | Field OTP giới hạn đúng 4 chữ số như nhãn "Mã OTP (4 số)" (inventory: không có `maxLength`/`pattern` — ForgotPassword.jsx:71-77) | Không nhập được quá 4 ký tự / ký tự không phải số | FR-22 (format constraints) |
| GUI-IA02-10 | Chi tiết SP | Input Số lượng có ràng buộc min/max (inventory: `type="number"` không có `min` — ProductDetail.jsx:57-62); thử nhập 0, -1, để trống, gõ chữ | Giá trị < 1 hoặc rỗng bị chặn/chuẩn hoá về 1; không thêm được NaN vào giỏ | FR-22 (format constraints: quantity) |
| GUI-IA02-11 | Thanh toán | Tổng tiền thanh toán là giá trị **chỉ đọc**, không phải input sửa được (inventory: `type="number"` editable, giá trị sửa gửi thẳng lên API — Checkout.jsx:94-103, 44-48) | Người dùng không sửa được tổng tiền trên UI | Heuristic (input constraint) — nghiêm trọng |
| GUI-IA02-12 | Thanh toán | Mã giảm giá chuẩn hoá hoa/thường nhất quán: nhập "sale10" xử lý như "SALE10" (inventory: CSS `uppercase` + `.toUpperCase()` khi gửi — Checkout.jsx:110-116, 30) | Mã không phân biệt hoa thường, hiển thị nhất quán chữ hoa | Heuristic (format constraints) |
| GUI-IA02-13 | Hồ sơ | Field Email hiển thị rõ trạng thái không sửa được: nhãn "(Không đổi)", nền xám, không focus/gõ được (inventory: `disabled` + `bg-gray-100` — Profile.jsx:117-125) | Field disabled đúng chuẩn, không nhận input | FR-22 (disabled state) — kiểm chứng pass |
| GUI-IA02-14 | Đăng ký | Form có field "Xác nhận mật khẩu" và bắt buộc khớp với field Mật khẩu (inventory: không tồn tại field này — Register.jsx:35-81) | Có field xác nhận; không khớp → lỗi hiện trên nút submit | Heuristic (confirmation-field matching) |
| GUI-IA02-15 | Đăng ký, Thanh toán, Hồ sơ | Thứ tự Tab trong form đi đúng thứ tự thị giác các field → nút submit cuối cùng, không có `tabIndex` phá thứ tự (Đăng nhập đã cover ở GUI-IA01-13) | Tab tuần tự từ field đầu đến nút submit | FR-21/FR-22 (tab order within forms) |
| GUI-IA02-16 | Đăng ký, Đăng nhập, Quên MK | Validation timing & ngôn ngữ nhất quán: form dựa vào HTML5 `required` native (Register.jsx:43,53,63; Login.jsx:34,44) → submit khi để trống hiện tooltip theo ngôn ngữ trình duyệt (có thể là "Please fill out this field" tiếng Anh), trong khi lỗi custom là tiếng Việt trong trang | Thông báo bắt buộc nhập nhất quán tiếng Việt, cùng style với lỗi khác | Heuristic (validation timing/consistency) |

**Coverage FR-22:** dấu `*` (01) · type email (02) · type password (03) · lỗi trên submit — anti-convention (04, 06) · step indicator (05) · format constraints (07-10, 12) · validation timing (16) · ngoài FR-22 (11, 13, 14, 15).
**Ghi chú:** aspect "CSV format" trong prompt không áp dụng — đã rà toàn bộ inventory, app không có input CSV nào.
